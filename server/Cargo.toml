[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
dotenvy = "0.15.7"
rand = "0.10.1"
rocket = { version = "0.5.1", features = ["json", "secrets"] }
uuid = { version = "1.3.1", features = ["v4"] }
